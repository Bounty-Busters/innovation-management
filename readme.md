Sa dati run:

```bash
cd medapp && ./gradlew bootRun
```

si se populeaza baza de date H2 in-memory cu tot ce trebuie din
`medapp/src/main/java/ro/medfinder/medapp/config/DataLoader.java`

La `/admin/` te poti loga cu
mail: `admin@medfinder.ro`
pass: `admin123`
